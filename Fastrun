#!/usr/bin/env bash
cd $HOME/TSHAKEA
rm -rf $HOME/.telegram-cli
install() {
rm -rf $HOME/.telegram-cli
sudo chmod +x tg
chmod +x TSHAKEA
chmod +x Be
./Be
}
if [ "$1" = "ins" ]; then
install
fi
chmod +x Run
lua Run.lua
